<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.component.login.Hilt_LoginActivity">

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:lottie_loop="true"
        app:lottie_fileName="bg_login.json"
        app:lottie_autoPlay="true"
        android:scaleType="fitXY"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/top"
        android:layout_width="300dp"
        android:layout_height="200dp"
        app:lottie_loop="false"
        app:lottie_fileName="top_login.json"
        app:lottie_autoPlay="true"
        android:layout_marginStart="@dimen/space_24x"
        android:layout_marginTop="@dimen/space_96x"
        android:layout_marginEnd="@dimen/space_24x"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/phone_logo"
        android:layout_width="40dp"
        android:layout_height="54dp"
        android:layout_marginStart="@dimen/space_24x"
        android:layout_marginTop="@dimen/space_400x"
        android:src="@drawable/ic_phone"
        android:scaleType="center"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <EditText
        android:id="@+id/phone_edit"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="@string/prompt_phone"
        android:inputType="phone"
        android:selectAllOnFocus="true"
        android:theme="@style/EditText_Style"
        android:textColor="@color/white"
        android:layout_marginEnd="@dimen/space_24x"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/phone_logo"
        app:layout_constraintBottom_toBottomOf="@+id/phone_logo" />

    <ImageView
        android:id="@+id/password_logo"
        android:layout_width="40dp"
        android:layout_height="54dp"
        android:src="@drawable/ic_password"
        android:scaleType="center"
        android:layout_marginTop="@dimen/space_16x"
        app:layout_constraintStart_toStartOf="@+id/phone_logo"
        app:layout_constraintTop_toBottomOf="@+id/phone_logo" />

    <EditText
        android:id="@+id/password_edit"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/space_24x"
        android:hint="@string/prompt_password"
        android:imeActionLabel="@string/action_sign_in_short"
        android:imeOptions="actionDone"
        android:inputType="textPassword"
        android:selectAllOnFocus="true"
        android:theme="@style/EditText_Style"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/password_logo"
        app:layout_constraintBottom_toBottomOf="@+id/password_logo" />

    <TextView
        android:id="@+id/login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/space_16x"
        android:text="@string/action_sign_in"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:background="@drawable/background_button_login_selector"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:paddingStart="48dp"
        android:paddingEnd="48dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/password_edit"
        app:layout_constraintVertical_bias="0.2" />

    <TextView
        android:id="@+id/tip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="@dimen/space_24x"
        android:layout_marginTop="@dimen/space_16x"
        android:layout_marginEnd="@dimen/space_24x"
        android:layout_marginBottom="@dimen/space_64x"
        android:textColor="@color/white_bb"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <RelativeLayout
        android:id="@+id/loader_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true" />
    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>